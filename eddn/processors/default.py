import logging

log = logging.getLogger(__name__)


def process(schema, message) -> None:
    pass
    # log.debug(f'Unsupported message found: {schema}')
    # log.debug('=' * 80)
    # log.debug(message)
    # log.debug('=' * 80)
